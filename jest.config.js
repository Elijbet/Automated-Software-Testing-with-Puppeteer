export default { // old way would be module.exports = {}
    rootDir: "./tests-snapshots",
    testTimeout: 30000,
    bail: 0 // bail: 1 would terminate if there was a failing test, 0 will keep going
  };